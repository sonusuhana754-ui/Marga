# Copy to .env.local to override. All optional — the app runs on mock data by default.

# Set to "false" to hit the real backend instead of the bundled mock fixtures.
VITE_USE_MOCK=true

# Backend base URL, used only when VITE_USE_MOCK=false.
VITE_API_BASE=http://localhost:8000/api/v1
